spring:
  application:
    name: ConfigServer
  profiles:
    active: git
  cloud:
    config:
      server:
        git:
          uri: https://github.com/SachinBhagat1617/OfssBankConfig
          username: ${USERNAME}
          password: ${PASSWORD}

server:
  port: 5555

